async function main(): Promise<void> {
  console.log('Deriv AI Recruiter — pipeline not yet implemented.');
  console.log('Run each step to build the pipeline incrementally.');
}

main().catch((err: unknown) => {
  console.error(err);
  process.exit(1);
});
